  <%= form_tag sort_cars_path, class: "form-search"  do  %>
  <% @sort_type = Car::LIKES_COUNT unless @sort_type %> 
    <%= select_tag :sort_type, options_for_select(Car::ORDER_TYPES, @sort_type) %> 
    <%= submit_tag "Sort", class: "btn btn-primary", :name => nil %> 
  <% end %> 
